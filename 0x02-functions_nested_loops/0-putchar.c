#include "main.h"
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	String x = "_putchar";
	int i;
	for(i = 0; i < strlen(str); i++)
	{
		_putchar(x[i]);
	}
	_putchar('\n');
	return (0);
}
