#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	string x = "_putchar";
	for(int i = 0; i < strlen(str); i++)
	{
		_putchar(x[i]);
	}
	_putchar('\n');
	return (0);
}
